from django import forms


#class MessageForm(forms.Form):
 #   receiver = forms.CharField(label='Receiver', max_length=100)
  #  title = forms.CharField(label='Receiver', max_length=100)
   # content = forms.Textarea(label='Content')
